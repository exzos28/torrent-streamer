# Анализ torrents.svg - Что показывает визуализация

## 📊 Общая информация

`torrents.svg` - это SVG-визуализация всех активных торрентов в системе, доступная по эндпоинту `/debug/torrents.svg`.

## 🎨 Структура визуализации

### 1. Легенда (Legend)
Вверху SVG отображается легенда с 4 состояниями кусков:

- **Светло-серый** (`piece-missing`) - Не загружен, не в приоритете
- **Синий** (`piece-downloaded`) - Загружен, не в приоритете  
- **Оранжевый** (`piece-prioritized`) - Не загружен, но в приоритете
- **Темно-оранжевый/красный** (`piece-prioritized-downloaded`) - Загружен и в приоритете

Легенда автоматически размещается в 1-2 строки в зависимости от ширины SVG.

---

### 2. Информация о каждом торренте

Для каждого активного торрента отображается:

#### 2.1. Название торрента
- **Расположение:** Первая строка, жирный шрифт
- **Содержимое:** Имя торрента (из метаданных)

#### 2.2. Статистика загрузки
- **Расположение:** Вторая строка
- **Формат:** `XX.X% | XXX.X KB/s | N peers`
- **Содержимое:**
  - Процент загрузки (0-100%)
  - Скорость загрузки (B, KB, MB, GB)
  - Количество пиров

#### 2.3. Статистика кусков (Pieces)
- **Расположение:** Третья строка
- **Формат:** `Pieces: X/Y downloaded | Z prioritized` (если есть приоритетные)
- **Содержимое:**
  - Количество загруженных кусков / общее количество кусков
  - Количество приоритетных кусков (если > 0)

#### 2.4. Прогресс-бар
- **Расположение:** Четвертый элемент
- **Визуализация:**
  - Серая полоса (фон) - полный размер
  - Зеленая полоса - загруженная часть
  - Скругленные углы (rx="4")

#### 2.5. Визуализация кусков (Pieces Bar)
- **Расположение:** Пятый элемент, под прогресс-баром
- **Визуализация:** Горизонтальная полоса, где каждый сегмент представляет один или несколько кусков
- **Цвета:**
  - Светло-серый - кусок не загружен и не в приоритете
  - Синий - кусок загружен, но не в приоритете
  - Оранжевый - кусок не загружен, но в приоритете (будет загружен в первую очередь)
  - Темно-оранжевый/красный - кусок загружен и в приоритете

---

## 🔍 Детали реализации

### Масштабирование кусков
- Максимум отображается **400 кусков** для читаемости
- Если кусков больше, используется `pieceStep` для группировки
- Каждый визуальный сегмент может представлять несколько кусков

### Источник данных
Данные берутся из `TorrentDebugInfo`, который включает:
- `pieces: number[]` - массив 0/1, где 1 = кусок загружен
- `prioritizedPieces: number[]` - массив 0/1, где 1 = кусок в приоритете
- `totalPieces: number` - общее количество кусков
- `progress: number` - общий прогресс (0-1)
- `downloadSpeed: number` - скорость загрузки в байтах/сек
- `numPeers: number` - количество подключенных пиров

### Настройки через query параметры
- `?width=1200` - ширина SVG (по умолчанию 800px)
- `?barHeight=40` - высота прогресс-бара (по умолчанию 30px)
- `?pieceBarHeight=10` - высота полосы кусков (по умолчанию 8px)

---

## 📈 Что можно увидеть на визуализации

### 1. Общий прогресс загрузки
- Зеленый прогресс-бар показывает процент загрузки
- Статистика кусков показывает точное количество

### 2. Паттерн загрузки
- **Последовательная загрузка:** Синие блоки идут подряд от начала
- **Случайная загрузка:** Синие блоки разбросаны по всей полосе
- **Приоритетная загрузка:** Оранжевые блоки показывают, какие куски будут загружены в первую очередь

### 3. Приоритетные куски
- Оранжевые сегменты показывают куски, которые были выбраны через `torrent.select()`
- Темно-оранжевые/красные сегменты - куски, которые уже загружены и были в приоритете
- Это полезно для отладки стратегии загрузки при стриминге

### 4. Проблемы загрузки
- Большие серые области - куски, которые еще не загружены
- Если оранжевые куски долго не становятся синими - возможны проблемы с пирами или сетью

---

## 🎯 Практическое применение

### Для отладки:
1. **Проверка стратегии загрузки:** Видно, загружаются ли куски последовательно или случайно
2. **Мониторинг приоритетов:** Видно, какие куски в приоритете и загружаются ли они
3. **Выявление проблем:** Большие серые области могут указывать на проблемы с пирами
4. **Оптимизация стриминга:** Можно увидеть, правильно ли приоритизируются куски для стриминга

### Для мониторинга:
1. **Прогресс в реальном времени:** Обновление страницы показывает текущее состояние
2. **Множественные торренты:** Видно состояние всех активных торрентов одновременно
3. **Статистика:** Скорость загрузки и количество пиров для каждого торрента

---

## 🔄 Обновление данных

- SVG генерируется при каждом запросе
- Заголовок `Cache-Control: no-cache` предотвращает кеширование
- Данные берутся из `getDebugInfo()`, который использует актуальное состояние торрентов

---

## 📝 Пример использования

```bash
# Открыть в браузере
open http://localhost:3000/debug/torrents.svg

# С настройками
open "http://localhost:3000/debug/torrents.svg?width=1200&pieceBarHeight=12"

# Скачать SVG
curl "http://localhost:3000/debug/torrents.svg" -o torrents.svg
```

---

## 🎨 Цветовая схема

| Цвет | Класс CSS | Значение |
|------|-----------|----------|
| Светло-серый | `piece-missing` | `#f5f5f5` - Не загружен, не в приоритете |
| Синий | `piece-downloaded` | `#2196f3` - Загружен, не в приоритете |
| Оранжевый | `piece-prioritized` | `#ff9800` - Не загружен, в приоритете |
| Темно-оранжевый | `piece-prioritized-downloaded` | `#ff5722` - Загружен и в приоритете |
| Зеленый | `progress-fill` | `#4caf50` - Прогресс загрузки |
| Серый | `progress-bg` | `#e0e0e0` - Фон прогресс-бара |

---

## 💡 Выводы

`torrents.svg` предоставляет **визуальное представление состояния всех активных торрентов**, показывая:
- ✅ Общий прогресс загрузки
- ✅ Детальное состояние каждого куска
- ✅ Приоритетные куски (для стриминга)
- ✅ Статистику (скорость, пиры)
- ✅ Паттерны загрузки (последовательная/случайная)

Это мощный инструмент для **отладки и мониторинга** работы торрент-стримера.
